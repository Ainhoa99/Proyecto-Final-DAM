<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProyectoFinalDam" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ProyectoFinalDam" parent="Base.Theme.ProyectoFinalDam" />

    <style name="HelperTextRed" parent="TextAppearance.Design.HelperText">
        <item name="android:textColor">@color/red</item> <!-- Cambia "@color/red" al color rojo deseado -->
    </style>
</resources>